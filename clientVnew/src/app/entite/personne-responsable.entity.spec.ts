import { PersonneResponsable } from './personne-responsable.entity';

describe('PersonneResponsable', () => {
  it('should create an instance', () => {
    expect(new PersonneResponsable()).toBeTruthy();
  });
});
